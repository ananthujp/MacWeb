import React from "react";

function Folders() {
  return (
    <div
      onClick={() => console.log("ll")}
      className="absolute right-4 top-8 w-auto"
    ></div>
  );
}

export default Folders;
